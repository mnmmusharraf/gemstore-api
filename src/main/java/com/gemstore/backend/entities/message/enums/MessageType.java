package com.gemstore.backend.entities.message.enums;

public enum MessageType {
    TEXT,       // Plain text message
    IMAGE,      // Image attachment
    FILE,       // File attachment
    LISTING     // Gem listing share
}